numpy>=1.24.0
scipy>=1.11.0
matplotlib>=3.7.0
scikit-image>=0.19.0
Pillow>=9.0.0
torch==1.12.1+cu113
tqdm>=4.60.0
thop>=0.1.0
protobuf>=3.20.0
typing-extensions>=4.0.0

# Optional (only if you use torchvision utilities)
# torchvision==0.13.1+cu113

# Notes:
# - Install PyTorch with the proper CUDA wheel for your system, e.g.:
#   pip install --extra-index-url https://download.pytorch.org/whl/cu113 torch==1.12.1+cu113
# - `scipy` provides `scipy.io` and `scipy.ndimage` used in the code.

